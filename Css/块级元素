块级元素特点：
         1、自己独占一行
         2、元素的宽度、高度、行高、边距都是可以设置的，如果不设置的话，默认为父元素的宽度
         3、常见的块级元素：div,p,h1-h6,ul,ol,table,from,address,section,header,footer

行级元素：
         1、可以和其他元素在一行
         2、元素的宽度、高度、边距不可设置
         3、元素的宽度就是包含的文字，图片的宽度，不可改变
         4、span,img,a,input,textarea,select,button