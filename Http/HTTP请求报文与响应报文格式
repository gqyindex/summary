请求报文三部分：
            1、请求行：请求方法，URL，HTTP版本信息
            2、请求头部字段（客户端的IP地址，服务器的主机名和端口号，客户端及其的信息，字符集，发送的语言，cookie）
            3、请求内容

响应报文三部分：
            1、状态行：HTTP版本、状态码、状态码的详情
            2、响应头部字段
            3、响应内容